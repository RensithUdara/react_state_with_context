function Section2({ theme }) {
    return (
        <div style={{
            backgroundColor: theme === "dark" ? "le293b" : "white"
        }}>
            <h2>Section 2</h2>
        </div >
    );
}

export default Section2;